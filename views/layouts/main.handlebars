<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WAGERLESS</title>
    
    {{!-- Google fonts imports --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nabla:EDPT,EHLT@163,16&display=swap" rel="stylesheet">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Madimi+One&display=swap" rel="stylesheet">

    {{!-- Bootstrap Icons CSS --}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    
    {{!-- Google Icon --}}
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />

    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="border-bottom">
        <ul class="nav nav-pills nav-fill fs-5">
            <li class="nav-item">
              <a class="nav-link" href="/homepage">HOME</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/communityforum">COMMUNITY FORUM</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/leaderboard">LEADERBOARD</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/support">SUPPORT</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/profile">PROFILE</a>
            </li>
            {{#if logged_in}}
            <li class="nav-item">
                <button class="nav-link" id="logout">LOGOUT</button>
            </li>
            {{else}}
            <li class="nav-item">
                <a class="nav-link" href="/login">LOGIN</a>
            </li>
            {{/if}}
          </ul>
    </nav>

{{#if isLoginPage}}
{{else if isSignupPage}}
{{else if isCommunityForum}}
{{else if isAddPost}}
{{else if isPostPage}}
{{else if isProfilePage}}
{{else if isSupportPage}}
{{else if isLeaderboardPage}}
{{else if isRpsPage}}
{{else}}
<main class="d-flex justify-content-center align-items-center vh-100"> 
    <div class="mainTitle text-center">
        <h1 class="title mb-3">WAGERLESS</h1>
        <p class="lead">"A rock, paper and scissors game to battle gambling addiction"</p>
        <div class="buttons1">
        {{#if logged_in}}
            <div class="text-center">
                <a href="/match" id="playBtn" class="btn btn-dark mb-3 btn-lg">PLAY</button></a>
            </div>
        {{else}}
            <div class="row">
                <div class="col text-center">
                    <a class="btn btn-dark mb-3 btn-lg" href="/login" id="loginBtn">LOGIN</a>
                </div>
            </div>
            <div class="row">
                <div class="col text-center">
                    <a href="/signup" id="signUpBtn" class="btn btn-light btn-lg">SIGN UP</a>
                </div>
            </div>
        {{/if}}    
        </div>
    </div>

</main>  

{{/if}}

{{{ body }}}

{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}

</body>
</html>